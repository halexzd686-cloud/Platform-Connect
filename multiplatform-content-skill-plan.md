# 多平台文案与配图适配 Skill 制作方案

## 1. 项目目标

制作一个面向中文内容创作者的 Codex Skill。用户输入一篇原始文章后，Skill 能够：

1. 提炼文章事实、观点、受众和作者立场。
2. 将文章拆成适合视频化传播的内容单元。
3. 根据不同平台的内容习惯生成对应文案。
4. 为每个平台规划封面、关键帧和正文配图。
5. 调用图像生成能力产出实际图片。
6. 对文案与图片执行一致性、事实性和视觉质量检查。
7. 按平台整理成可继续编辑或发布的内容包。

这个 Skill 的目标不是简单地把同一篇文章缩短成几个版本，而是完成“原始文章 → 内容母版 → 平台适配 → 视觉策划 → 文案与图片交付”的完整流程。

## 2. 核心设计原则

### 2.1 先建立内容母版，再做平台改写

所有平台版本必须从同一份内容母版派生，避免分别改写造成观点漂移、事实变化或平台版本互相矛盾。

内容母版至少包含：

- 核心观点
- 作者立场
- 目标读者
- 事实、数据与案例
- 可拆分的内容单元
- 可强化的表达
- 禁止编造或改变的内容
- 原文语气
- 适合视频化的认知锚点

### 2.2 平台适配不等于控制字数

每个平台使用独立适配器。适配器应控制：

- 内容切入角度
- 开头钩子
- 叙事节奏
- 口语化程度
- 建议时长
- 标题与封面文字
- 发布文案
- 互动方式与 CTA
- 视觉比例和配图用途
- 平台合规与原创要求

平台习惯、尺寸和发布规则可能变化，因此将它们放入独立参考文件，并标注更新时间，不在 `SKILL.md` 中硬编码大量易过期信息。

### 2.3 文案和图片共用同一组语义锚点

视觉模块不得重新总结文章。它必须读取平台文案已经确定的：

- 核心钩子
- 关键结论
- 封面短句
- 视觉关键词
- 禁止修改的事实

这样可以保证标题、口播稿、封面和配图表达一致。

### 2.4 保持渐进式加载

`SKILL.md` 只放核心工作流和资源导航。各平台规则、视觉风格、提示词模板、输出结构等内容放在 `references/` 中按需读取，避免一次加载全部资料。

## 3. 总体工作流

```text
原始文章
  ↓
内容母版（事实、观点、受众、语气、边界）
  ↓
内容切片（钩子、问题、案例、方法、结论）
  ↓
平台选择与适配
  ├─ 抖音
  ├─ 小红书
  ├─ 视频号
  └─ B站
  ↓
各平台文案包
  ↓
各平台视觉清单 visual manifest
  ↓
逐张生成封面、关键帧和配图
  ↓
事实 QA、平台 QA、视觉 QA、一致性 QA
  ↓
文件化交付
```

## 4. 第一版支持的平台

MVP 首先支持以下四个平台：

### 4.1 抖音

默认产物：

- 3 个开头钩子
- 短视频口播稿
- 逐句字幕稿
- 标题候选
- 发布文案
- 封面短句
- 竖屏镜头或关键帧清单
- 评论区互动引导

### 4.2 小红书

默认产物：

- 标题候选
- 视频口播稿
- 笔记正文
- 分段小标题
- 话题建议
- 封面短句与封面方案
- 适合图文或视频使用的配图清单

### 4.3 视频号

默认产物：

- 口播稿
- 标题候选
- 发布说明
- 朋友圈转发文案
- 信任型或观点型封面方案
- 评论区互动引导
- 关键帧与配图清单

### 4.4 B站

默认产物：

- 中长视频脚本
- 开场与内容章节
- 标题候选
- 视频简介
- 封面文字与封面方案
- 动态发布文案
- 章节插图或重点解释图清单

后续可增加快手、西瓜视频、TikTok、YouTube Shorts、微信公众号等适配器。

## 5. 配图能力融合方案

参考素材：`E:/Downloads/ian-xiaohei-illustrations-main.zip`

现有配图 Skill 的价值主要在于以下工作链：

1. 从文章中寻找认知锚点。
2. 为认知锚点选择视觉结构。
3. 将抽象概念转化为物理动作或视觉隐喻。
4. 让角色参与核心动作。
5. 为单张图片生成结构化提示词。
6. 生成后按 QA checklist 检查并迭代。

### 5.1 从固定文章配图升级为视觉资产策划

原 Skill 默认生成 16:9 正文插图。新 Skill 应根据平台和用途先生成 `visual manifest`，再决定比例、构图和文字量。

每个视觉资产至少记录：

```yaml
platform: douyin
asset_type: opening-keyframe
purpose: 前三秒解释核心冲突
aspect_ratio: 9:16
source_anchor: 内容母版中的观点编号
core_idea: 这张图只表达的一个意思
visual_metaphor: 使用的物理隐喻
character_action: 角色承担的核心动作
labels:
  - 短标注1
  - 短标注2
status: planned
```

### 5.2 将“小黑”作为第一套视觉预设

建议将现有风格封装为：

```text
visual_style: xiaohei
```

小黑仅作为可选视觉预设之一，不设默认风格。系统应先根据文章行业、表达任务和平台用途推荐 3–5 个方向，再由用户选择，例如：

- `xiaohei`
- `minimal-editorial`
- `bold-poster`
- `clean-tech`
- `brand-custom`

### 5.3 小黑视觉预设需要保留的规则

- 纯白背景
- 黑色手绘线稿
- 大量留白
- 少量红、橙、蓝中文标注
- 一张图只解释一个结构或认知动作
- 小黑必须承担核心动作，不能只是装饰
- 避免 PPT、正式流程图、商业插画和儿童卡通感
- 每次根据当前内容重新创造隐喻，不复刻样例构图
- 图片中文字尽量少而短

### 5.4 图片类型

视觉模块需要区分：

- 视频封面
- 前三秒关键帧
- 视频中的观点解释图
- 流程或方法解释图
- 章节过渡图
- 图文笔记分页图
- 文章正文插图

不同图片类型使用不同构图要求，不能用同一张 16:9 图片简单裁切到所有平台。

## 6. 运行模式

Skill 支持三种主要运行模式：

### `plan`

只分析文章，输出内容母版、平台适配策略和视觉清单，不生成最终文案与图片。

### `copy`

生成内容母版和各平台文案，不生成图片。

### `full`

生成内容母版、平台文案、视觉清单和最终图片，并执行完整 QA。

建议支持以下控制参数：

```yaml
mode: full
platforms:
  - douyin
  - xiaohongshu
  - wechat-channels
  - bilibili
visual_style: xiaohei
generate_images: true
```

如果用户只提供文章而没有说明平台，Skill 必须先询问“想要哪几个平台的文案和配图”。默认不选择任何平台；支持单个平台、任意组合和用户明确要求的全平台。海外平台还需确认目标语言和市场。

## 7. 建议的 Skill 目录

建议 Skill 名称：`adapt-content-for-platforms`

```text
adapt-content-for-platforms/
├── SKILL.md
├── agents/
│   └── openai.yaml
├── references/
│   ├── content-brief-schema.md
│   ├── platform-selection.md
│   ├── output-schema.md
│   ├── douyin.md
│   ├── xiaohongshu.md
│   ├── wechat-channels.md
│   ├── bilibili.md
│   ├── visual-planning.md
│   ├── xiaohei-style.md
│   ├── composition-patterns.md
│   ├── image-prompt-template.md
│   ├── qa-checklist.md
│   └── third-party-notices.md
├── scripts/
│   ├── prepare_workspace.py
│   ├── validate_manifest.py
│   └── build_delivery_index.py
└── assets/
    └── xiaohei-examples/
```

为避免引用层级过深，所有参考文件直接由 `SKILL.md` 链接。平台文件和视觉文件通过文件名区分，不继续建立多层嵌套目录。

## 8. 各文件职责

### `SKILL.md`

- 定义触发场景。
- 说明三种运行模式。
- 调度内容母版、平台适配、视觉生成和 QA。
- 告诉 Agent 在什么条件下读取哪个参考文件。
- 保持简洁，不塞入各平台的全部细节。

### `references/content-brief-schema.md`

- 定义内容母版字段。
- 规定事实保护、观点保护和不确定信息处理方法。

### `references/platform-selection.md`

- 说明如何根据内容类型选择平台。
- 说明用户未指定平台时的默认行为。

### 各平台参考文件

- 记录平台内容习惯。
- 定义该平台的文案产物。
- 记录默认结构、语气、互动方式和视觉用途。
- 记录需要定期核对的发布规格与规则。

### 视觉参考文件

- 定义视觉资产策划方式。
- 保存小黑风格 DNA、构图模式和生图提示词模板。
- 定义生成后视觉 QA。

### `scripts/`

只承担确定性任务，不负责创意写作：

- 创建输出目录。
- 生成和验证 manifest。
- 检查必填字段、文件命名和重复资产。
- 汇总各平台结果，生成交付索引。

真正的文章理解、平台改写、隐喻设计和图片生成由 Agent 与图像工具完成。

## 9. 输出目录

每篇文章生成独立交付包：

```text
outputs/<article-slug>/
├── source-brief.md
├── manifest.json
├── douyin/
│   ├── copy.md
│   └── images/
├── xiaohongshu/
│   ├── copy.md
│   └── images/
├── wechat-channels/
│   ├── copy.md
│   └── images/
├── bilibili/
│   ├── copy.md
│   └── images/
└── index.md
```

`manifest.json` 记录：

- 原始文章信息
- 运行模式
- 已选择平台
- 各文案文件路径
- 每张图片的用途、比例和对应内容锚点
- 生成状态
- QA 状态
- 需要人工确认的问题

## 10. QA 体系

### 10.1 事实 QA

- 数据与案例是否来自原文或可靠来源。
- 是否改变作者观点。
- 是否出现原文不存在的承诺、因果或结论。
- 不确定信息是否被明确标注。

### 10.2 平台 QA

- 文案是否真正符合平台表达场景。
- 是否只是机械缩短原文。
- 标题、开头和 CTA 是否与正文一致。
- 是否满足原创、版权和平台安全要求。

### 10.3 视觉 QA

- 比例是否符合计划用途。
- 一张图是否只表达一个核心意思。
- 图片文字是否少、短、可读。
- 角色是否承担核心动作。
- 是否出现错字、无关元素、过度拥挤或风格漂移。
- 是否错误复刻已有样例。

### 10.4 跨资产一致性 QA

- 封面承诺是否能在视频脚本中得到兑现。
- 口播稿、发布文案和图片是否使用相同的核心结论。
- 不同平台版本是否仍然忠于同一内容母版。

## 11. 授权与来源处理

参考 ZIP 中的 `Ian Xiaohei Illustrations` 使用 MIT License。融合其提示词、说明和结构时，需要保留原版权声明与 MIT 许可文本。

其 `NOTICE.md` 还要求衍生版本保留项目名称或向 Ian 提供归属说明。因此实际 Skill 中建议加入 `references/third-party-notices.md`，写明：

- 原项目名称：Ian Xiaohei Illustrations
- 作者：Ian
- 原项目许可：MIT License
- 哪些视觉规则、提示词或样例来源于该项目
- 原作者提供的项目链接

如果直接包含原项目样例图，也需要保留对应归属说明。后续如果完全重写视觉规范并不再携带样例图，仍应根据实际复用程度判断是否属于 substantial portions。

## 12. 制作步骤

后续正式制作按以下顺序推进：

1. 确认 Skill 名称、安装位置、默认用户和默认行为。
2. 使用官方 `init_skill.py` 初始化 Skill。
3. 先编写内容母版 schema、平台适配文件和视觉参考文件。
4. 融合并改写小黑配图规则，补充授权说明。
5. 编写精简的 `SKILL.md` 作为总调度器。
6. 添加真正需要的确定性脚本。
7. 生成 `agents/openai.yaml`。
8. 使用 `quick_validate.py` 验证目录、命名和 frontmatter。
9. 使用同一篇文章测试四个平台的 `plan`、`copy`、`full` 模式。
10. 检查事实一致性、平台差异和图片效果，迭代 Skill。

## 13. MVP 验收标准

第一版满足以下条件即可认为可用：

- 输入一篇中文文章后能生成结构化内容母版。
- 能为抖音、小红书、视频号和 B站生成明显不同的文案包。
- 各平台版本不改变原始事实和作者核心立场。
- 能输出每个平台对应的 visual manifest。
- 能使用小黑风格逐张生成图片，而不是把多张图拼在一张里。
- 图片比例和用途匹配平台交付场景。
- 能生成清晰的输出目录和索引。
- 支持只规划、只生成文案和完整生成三种模式。
- 能发现并报告事实漂移、图片错字和视觉风格问题。

## 14. 当前尚待确认的产品决策

以下内容在正式创建 Skill 前需要最终确认：

1. 已确认：覆盖多行业内容，不限定知识类个人创作者；视觉模块按行业路由并保留未列行业的回退方法。
2. 已确认：用户只输入文章时，先多选目标平台；不默认生成全部平台。
3. 已确认：小黑是可选视觉预设，不是默认固定风格。
4. 已确认：先以项目内可分发 Skill 交付；是否复制到 Codex 全局 Skills 目录由用户另行决定。
5. 已确认：先生成并编辑配图清单，只有用户明确确认后才逐张生成所选平台图片。

在这些决策确认前，可以先完成不会受其影响的内容母版 schema、平台适配框架和 visual manifest 设计。
